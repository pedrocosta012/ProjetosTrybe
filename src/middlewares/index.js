const validateId = require('./validateId');
const validateName = require('./validateName');
const validatePostProduct = require('./validatePostProduct');

module.exports = { validateId, validateName, validatePostProduct };
